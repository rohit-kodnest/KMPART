
# KMPART
An e-commerce platform similar to Flipkart designed with React.

## Running the App
1. Run `npm install` to install dependencies.
2. Start the development server with `npm start`.

To get this project up and running, download the files, run `npm install` to install all dependencies, and then `npm start` to begin the development server. This will open the application in your default web browser.